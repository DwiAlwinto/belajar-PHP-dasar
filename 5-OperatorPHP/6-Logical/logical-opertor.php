<?php
    //! 1. And ($x and $x < 10)  (bisa menggunakan ini)  && ($x > 1 && $x < 10)
    //! 2. Or ($x > 1 or $x < 10)  (bisa menggunakan ini) || ($x > 1 || $x < 10)
    //! 3. Xor ($x > 1 xor $x < 10) (aturnya : return true jika salah satu nya benar, tapi  bukan keduanya benar);
    //! 4. Not($x != 3) (ini tidak sama dengan)

    //! and atau && : (ini semua kondisional harus bernilai true) kedua kondisi harus benar.
    // $a = 5;
    // $b = 10;
    // var_dump($a > 2 and $b > 2); //ini akan true karena nilai kondisi ya benar semuanya.
    //echo "<br>";
    // $a = 5; 
    // $b = 6;
    // var_dump($a > 10 and $b < 7);  //ini jawabanya akan false karena ada salah tidak benar kondisinya aturan harus benear.

    //! or atau || (or atau || (kondisinya salah satu harus true, baru bernilai true))
    // $a = 5;
    // $b = 10;
    // var_dump($a > 1 || $b > 1);  // (ini kondisi nya benar semua nilai ya true);

    // echo "<br>";
    // echo "<br>";

    // $a = 5;
    // $b = 10;
    // var_dump($a > 1 || $b > 11);  // (ini kondisi true 1 dan false 1 : jadinya  nilanya masih true);


    // ! xor aturanya : salah satu harus benar, kalau semua ya bener akan salah. 
        // $a = 5;
        // $b = 10;
        // var_dump($a > 2 xor $b > 2); // ini akan false jawabanya.(kareana nilaiya benar semuanya).

        // $a = 5;
        // $b = 10;
        // var_dump($a > 2 xor $b > 10); // ini akan true jawabanya.(karena nilai salah dari konidisinya ada yang salah).
        echo"<br>";
        
    //  ! != (tidak sama dengan)
        // $a = 10;
        // $b = 15;
        // var_dump($a != 10);












